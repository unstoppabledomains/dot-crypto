pragma solidity ^0.5.0;

import "truffle/Assert.sol";

contract TestResolver {
  function test() external {
    // Resolver resolver = new Resolver();
    // Simple proxy = Simple(address(new Proxy(address(simple))));
  }
}
